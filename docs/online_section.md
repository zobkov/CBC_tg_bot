# Структура ТГ-бота для Онлайн лекций 
*Отдел Амбассадоров*

Диалог: online_dialog

Папка диалога: app/bot/dialogs/online/

## DB
Необходимо две таблицы в БД (в дополнение к существующим):
- online_events
- online_events_registrations

**online_events**
```sql
id          SERIAL PRIMARY KEY,
slug        VARCHAR(100) UNIQUE NOT NULL,   -- "lecture_1337", ключ для синхронизации с конфигом
alias       VARCHAR(50) NOT NULL,            -- короткое имя для inline-кнопок
title       VARCHAR(255) NOT NULL,
speaker     VARCHAR(255),
description TEXT,
url         VARCHAR(500),
start_at    TIMESTAMP NOT NULL,              -- start_date + start_time, хранится как UTC
end_at      TIMESTAMP NOT NULL,              -- end_date + end_time, хранится как UTC
is_active   BOOLEAN DEFAULT true,
created_at  TIMESTAMP DEFAULT NOW()
```
Таблица БД ивентов обновляется каждый раз при запуске бота. Обновляется из config/lectures.json по примерно такой схеме:
```sql
INSERT INTO events (slug, alias, title, speaker, description, url, start_at, end_at)
VALUES (...)
ON CONFLICT (slug) DO UPDATE SET
    title = EXCLUDED.title,
    speaker = EXCLUDED.speaker,
    description = EXCLUDED.description,
    url = EXCLUDED.url,
    start_at = EXCLUDED.start_at,
    end_at = EXCLUDED.end_at,
    alias = EXCLUDED.alias;
-- slug не трогаем, is_active не трогаем
```

**online_events_registrations**
```sql
id            SERIAL PRIMARY KEY
user_id       INTEGER REFERENCES users(id)
event_id      INTEGER REFERENCES events(id)
status        VARCHAR(20) DEFAULT 'active'  -- active / cancelled
registered_at TIMESTAMP DEFAULT NOW()

UNIQUE(user_id, event_id)
```

**ОЧЕНЬ ВАЖНО!!!** Перед началом имплементирования взаимодействия с БД необходимо ознакомиться с другими примерами из этого проекта. Важно не забывать про мидлварь, которая передает доступ апдейту к БД и занимается коммитами и роллбеками.

## States
`OnlineSG`:
- `MAIN`
- `SCHEDULE` (main entrypoint to schedule. Dynamicly adds schedule from config file (json))
- `schedule_event` (dynamic – depends on selected event and its configured details)
- `MY_EVENTS` (shows user registered events)
- `my_events_event` (shows user registered event)
- `SUPPORT` (shows online related FAQ and support contacts)
- `successful_registration` (success msg)
- `сalendar_addition` (generates and sends .ics file)

## Структура
### Главное меню 
**STATE:** `OnlineSG.MAIN`

Кнопки:
- 📆 Расписание лекций
- 📚 Мои лекции
- 🔍 Квиз
- Ряд: [- ❓ Помощь; - ⬅️ Назад]

---
### 1. Schedule
**STATE:** `OnlineSG.SCHEDULE`

Это меню формируется на основе данных БД. 

Списоком в сообщении идут мероприятия с аттрибутами из 1.1

Список идет обязательно в хронологическом порядке. Устаревшие мероприятие не отображаются спустя 3 часа после `start_at`
Это применимо и для текста в сообщении и для соответсвующих им кнопкам.

Структура текста для одного ивента:
```html
📝 {title}
🎙️ {speaker}

📅 {start_date}, {start_time} (МСК)
```
Также для каждого ивента кнопка:
`{start_date} {alias}`


В итоге в сообщении формируется полный список всех мероприятий через 3 строки.
```html
📝 {title}
🎙️ {speaker}

📅 {start_date}, {start_time} (МСК)



📝 {title}
🎙️ {speaker}

📅 {start_date}, {start_time} (МСК)
```


---
#### 1.1 Event card
Configurable from file: `lectures.json`

Path: `config/`

Event's attributes:
- `title` - название
- `description` - описание
- `speaker` - кто спикер/лектор/ведущий (или множественные)
- `start_at` - дата и времяначала 
- `end_at` - дата и время конца *(пока не используется)*
- `alias` - сокращенное название для кнопки **(!!!!)**
- `slug` - внутренний идентификатор (например, "lecture_1337")
- `url` - ссылка на мероприятие

Sample .json:
```json
{
    {
        "title" : "sample title",
        "speaker" : "speaker",
        "description" : "extended description",
        "start_at": "2025-10-23 10:50:00+03",
        "end_at": "2025-10-23 11:50:00+03",
        "alias": "title", // used for telegram inline buttons - must be short
        "slug" : "lecture_1337",
        "url": "url",
    }
}
```

Пример карточки:
```html
Лекция: <b>{title}</b>
Спикер: <b>{speaker}</b>

Дата и время: {start_date(derived from start_at)}, {start_time(derived from start_at)} (МСК)


Описание:
{description}

Ссылка на лекцию: 
🔗 {url}

{registration_status} <!-- текстовый статус регистрации (пустой, если регистрации нет. Иначе текст: ✅ Ты зарегестрирован на эту лекцию. Берем из геттера-->
```

**ВАЖНО** !!!  Все даты в конфиге в поясе UTC+3 Москва.

Также статус регистрации возвращается getter'ом. 

Общий формат даты и времени: 25 октября 2025, 17:00 (МСК)

Buttons:
- {✅ Зарегистрироваться // ❌ Отменить регистрацию} (depending on registration_status) – 1.1.1
- 🗓️ Добавить в календарь (если статус регистрации активно)
- ⬅️ Назад

---
##### 1.1.1 Event Registration
В зависимости от статуса регистрации два сценария:

1 -  Регистрируем
Добавляем регистрацию в таблицу `online_events_registrations` или обновляем статус существуеющей (это произойдет в случае, если регистрация была отменена)

После успешной регистрации переключаем на окно `OnlineSG.successful_registration`

###### 1.1.1.1 Successful registration
```plain
Поздравляем! Ты успешно зарегистрирован на лекцию {title} 🎉

Ссылка на трансляцию станет активна в карточке мероприятия за 1 час до начала мероприятия. Мы также пришлем тебе напоминание 🔔

Твои зарегистрированные лекции всегда можно найти в разделе «📚 Мои лекции»
```
И тут 2 КНОПКИ:
- 🗓️ Добавить в календарь (1.1.2)
- ⬅️ Назад


2 - Отменяем регистрацию

Меняем статус регистрации на `cancelled`

---
##### 1.1.2 Calendar addition (.ics)
Тут надо реализовать модуль генерации .ics файла для конкретного мероприятия. Он в ключается в работу, когда пользователь нажимает добавить в календарь. Это объект для отдельного ТЗ. 

---
### 2. My events
**STATE:** `OnlineSG.MY_EVENTS`

Бот динамически формирует список из лекций, где есть регистрация данного пользователя по аналогии со списком 1 расписание, но тут только лекции, на которые есть регистрация пользователя. 

#### 2.1 Карточка ивента
**STATE:** `OnlineSG.my_events_event`

Внизу крупно отображается СТАТУС лекции:
🔗 Ссылка будет доступна 25.02 в 16:00 (МСК) (если время начала > 1 часа от текущего) (соответсвенно тут время start_at - 1 hr)

🎥 Ссылка активна: [ссылка на трансляцию] (если до начала 1 час)

Кнопки под карточкой:
- «🔗 Получить ссылку» 
  - (кнопка показывается всегда, но она отправляет сообщение с ссылкой (простым сообщением aiogram) не ранее чем за час до мероприятия. Во время и после она тоже может быть активна. Это необходимо, что можно было получить ссылку без обновления состояния окна)
- «📆 Добавить в календарь» (1.1.2)
- «⬅️ Назад»

### 3. Quiz
**STATE:** `QuizDodSG.MAIN`

Просто туда пересылвает

### 4. Support
**STATE:** `OnlineSG.SUPPORT`

пока тут плейсхолдер